import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-6.2-active-bar-start-demo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
